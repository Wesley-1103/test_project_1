# -----------------------------------------------------------------------------
# Flutter Web 專案用 .gitignore
# 涵蓋 Dart/Flutter 建置產物、Web 部署工具目錄，以及環境變數等機敏檔案。
# -----------------------------------------------------------------------------

# 雜項與編輯器暫存
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code：若希望整份 .vscode 不納入版控，可取消下一行註解
#.vscode/

# Dart / Pub / Flutter 建置（含 Web 輸出於 build/web）
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication / Obfuscation
app.*.symbols
app.*.map.json

# Android 建置產物（若日後加入 Android 目標）
/android/app/debug
/android/app/profile
/android/app/release

# -----------------------------------------------------------------------------
# Web：靜態託管與前端工具鏈（避免誤提交本機或 CI 快取）
# -----------------------------------------------------------------------------
.vercel/
.netlify/
.firebase/
node_modules/

# -----------------------------------------------------------------------------
# 環境變數與機敏設定（勿將 API 金鑰、Token、憑證提交至 Git）
# 建議：僅提交 .env.example（範本變數名，不含真實值），實際 .env 由部署平台注入。
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
!.env.example
!.env.sample

# 常見雲端／簽章憑證檔名（依專案需要可再增刪）
**/google-services.json
**/GoogleService-Info.plist
*credentials*.json
*.pem
*.p12
*.jks
*.keystore
